define(['jquery'], function ($) {
    'use strict';

    console.log('This is just for testing.');

    // return function (config, element) {
    //     // console.log('This is just for testing and require js.', config);
    //     // console.log(element);
    //     $.getJSON(config.base_url + 'rest/V1/directory/currency', function (result) {
    //         element.innerText = JSON.stringify(result, null, 2);
    //     })
    // }
});